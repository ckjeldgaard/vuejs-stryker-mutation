# vuejs-stryker-mutation

Example project using Stryker with Vue.js components.

## Build Setup

``` bash
# install dependencies
npm install

# run the tests using Jest
npm test

# run stryker
npm run stryker
```